function newAttemptId(){if(globalThis.crypto?.randomUUID) return globalThis.crypto.randomUUID();return `att-${Date.now()}-${Math.random().toString(16).slice(2)}`;}
export function recordAttempt(state,{question,answer,responseMs,createdAt=new Date().toISOString()}){const correctAnswer=question.table*question.multiplier,isCorrect=Number(answer)===correctAnswer,table=state.tables[question.table],fact=table.facts[question.multiplier];table.attempts++;state.totalAttempts++;fact.attempts++;fact.last=createdAt;fact.recent.push(isCorrect);fact.recent=fact.recent.slice(-5);const normalizedResponseMs=Number.isFinite(Number(responseMs))?Math.max(0,Number(responseMs)):null;fact.lastResponseMs=normalizedResponseMs;if(normalizedResponseMs!==null){fact.responseTimes.push(normalizedResponseMs);fact.responseTimes=fact.responseTimes.slice(-20);}if(isCorrect){table.correct++;state.totalCorrect++;fact.correct++;}else{table.wrong++;state.totalWrong++;fact.wrong++;}return{attemptId:newAttemptId(),schemaVersion:1,table:question.table,multiplier:question.multiplier,answer:Number(answer),correctAnswer,isCorrect,responseMs:normalizedResponseMs,createdAt};}
