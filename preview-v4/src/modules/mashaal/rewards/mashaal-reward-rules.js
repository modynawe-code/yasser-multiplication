const perSession=(prefix,event)=>`${prefix}:${event.gameId}:${event.sessionId||event.at}`;

export const MASHAAL_REWARD_RULES=Object.freeze([
  Object.freeze({
    id:'attempt-flower',
    rewardId:'mashaal-attempt-flower',
    eventTypes:Object.freeze(['game.completed']),
    awardKey:event=>perSession('mashaal-attempt-flower',event)
  }),
  Object.freeze({
    id:'persistence-pearl',
    rewardId:'mashaal-persistence-pearl',
    eventTypes:Object.freeze(['game.retry']),
    awardKey:event=>perSession('mashaal-persistence-pearl',event)
  }),
  Object.freeze({
    id:'cooperation-heart',
    rewardId:'mashaal-cooperation-heart',
    eventTypes:Object.freeze(['game.cooperation.completed']),
    awardKey:event=>perSession('mashaal-cooperation-heart',event)
  }),
  Object.freeze({
    id:'achievement-ribbon',
    rewardId:'mashaal-achievement-ribbon',
    eventTypes:Object.freeze(['game.goal.reached']),
    awardKey:event=>perSession('mashaal-achievement-ribbon',event)
  }),
  Object.freeze({
    id:'courage-star',
    rewardId:'mashaal-courage-star',
    eventTypes:Object.freeze(['game.completed']),
    when:event=>event.payload?.firstCompletion===true,
    awardKey:event=>`mashaal-courage-star:${event.gameId}`
  }),
  Object.freeze({
    id:'progress-butterfly',
    rewardId:'mashaal-progress-butterfly',
    eventTypes:Object.freeze(['game.completed']),
    when:(_event,context)=>context?.developmentalProgressImproved===true,
    awardKey:(event,context)=>`mashaal-progress-butterfly:${context?.progressKey||event.sessionId||event.at}`
  }),
  Object.freeze({
    id:'consistency-star',
    rewardId:'mashaal-consistency-star',
    eventTypes:Object.freeze(['game.completed']),
    when:(_event,context)=>[3,7,14,30].includes(Number(context?.streakMilestone)),
    awardKey:(_event,context)=>`mashaal-consistency-star:${Number(context.streakMilestone)}`
  }),
  Object.freeze({
    id:'variety-rainbow',
    rewardId:'mashaal-variety-rainbow',
    eventTypes:Object.freeze(['game.completed']),
    when:(_event,context)=>Number(context?.uniqueGamesCompleted)>=3,
    awardKey:()=> 'mashaal-variety-rainbow:3-games'
  }),
  Object.freeze({
    id:'surprise-box',
    rewardId:'mashaal-surprise-box',
    eventTypes:Object.freeze(['game.completed']),
    when:(_event,context)=>[5,10,25].includes(Number(context?.completionMilestone)),
    awardKey:(_event,context)=>`mashaal-surprise-box:${Number(context.completionMilestone)}`
  }),
  Object.freeze({
    id:'distinction-crown',
    rewardId:'mashaal-distinction-crown',
    eventTypes:Object.freeze(['game.goal.reached']),
    when:(_event,context)=>context?.independentMilestone===true,
    awardKey:(event,context)=>`mashaal-distinction-crown:${context?.milestoneKey||event.sessionId||event.at}`
  }),
  Object.freeze({
    id:'mashaal-cup',
    rewardId:'mashaal-cup',
    eventTypes:Object.freeze(['game.completed']),
    when:(_event,context)=>Number(context?.completionMilestone)===25,
    awardKey:()=> 'mashaal-cup:25-completions'
  }),
  Object.freeze({
    id:'magic-wand',
    rewardId:'mashaal-magic-wand',
    eventTypes:Object.freeze(['game.completed']),
    when:(_event,context)=>context?.coreBehaviorsMilestone===true,
    awardKey:(event,context)=>`mashaal-magic-wand:${context?.milestoneKey||event.at}`
  })
]);
