import test from 'node:test';import assert from 'node:assert/strict';import {DOMINO_PIP_POSITIONS,dominoHalfMarkup,dominoFaceMarkup,dominoAssetKey,dominoAssetPath,enhanceDominoTile} from '../src/modules/games/domino/domino-visuals.js';
test('domino pip layouts match standard values 0 through 6',()=>{const expected={0:[],1:['mm'],2:['tl','br'],3:['tl','mm','br'],4:['tl','tr','bl','br'],5:['tl','tr','mm','bl','br'],6:['tl','ml','bl','tr','mr','br']};for(let value=0;value<=6;value++){assert.deepEqual([...DOMINO_PIP_POSITIONS[value]],expected[value]);assert.equal((dominoHalfMarkup(value).match(/class="domino-pip/g)||[]).length,value);}});
test('reference asset paths use the upstream max-min naming',()=>{assert.equal(dominoAssetKey(2,6),'6-2');assert.equal(dominoAssetPath(2,6),'assets/domino/tiles/6-2.svg');});
test('renderer covers the full double-six set with local reference assets',()=>{const tiles=[];for(let left=0;left<=6;left++)for(let right=left;right<=6;right++)tiles.push(dominoFaceMarkup(left,right));assert.equal(tiles.length,28);assert.equal(new Set(tiles).size,28);assert.ok(tiles.every(markup=>markup.includes('domino-reference-face')));});
test('face-down renderer exposes no pip values',()=>{const markup=dominoFaceMarkup(6,5,{faceDown:true});assert.match(markup,/domino-back-face/);assert.doesNotMatch(markup,/domino-pip/);});
test('invalid pip values are rejected',()=>{assert.throws(()=>dominoHalfMarkup(-1),RangeError);assert.throws(()=>dominoHalfMarkup(7),RangeError);});
test('visual enhancer marks doubles without deciding board orientation',()=>{const classes=new Set();const image={addEventListener(){}};const tile={dataset:{},children:[{tagName:'SPAN',textContent:'5'},{tagName:'I',textContent:''},{tagName:'SPAN',textContent:'5'}],classList:{toggle:(name,force)=>force?classes.add(name):classes.delete(name),add:name=>classes.add(name),remove:name=>classes.delete(name)},setAttribute(){},querySelector(){return image;},set innerHTML(value){this.rendered=value;}};assert.equal(enhanceDominoTile(tile),true);assert.equal(classes.has('is-double'),true);assert.equal(classes.has('vertical'),false);assert.match(tile.rendered,/assets\/domino\/tiles\/5-5\.svg/);});
