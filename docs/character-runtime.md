# Character runtime

Character art is a presentation asset only. Training, mastery, storage and reporting logic must never depend on image files.

Runtime references use semantic filenames under `assets/characters` and `assets/assistant`. Replacing art must not require changes to domain or application layers.

Student surfaces may use mascots for welcome, practice guidance and results. Exam mode and the parent dashboard remain visually quiet.
